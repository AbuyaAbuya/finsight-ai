import WorkInProgress from "../components/WorkInProgress";

function Settings() {
  return (
    <WorkInProgress
      title="Settings"
      description="The Settings module is under development. It will provide user management, company settings, currencies, security, permissions, and application configuration."
    />
  );
}

export default Settings;